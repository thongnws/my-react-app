export type Student = {
  id: string;
  name: string;
  schoolId: string;
  legalguardianId: string;
};

export type School = {
  id: string;
  name: string;
};

export type Legalguardian = {
  id: string;
  name: string;
};
